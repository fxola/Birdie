import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const wellbeingSlice = createSlice({
  name: 'welbeing',
  initialState,
  reducers: {},
  extraReducers: {},
});

export const {} = wellbeingSlice.actions;

export default wellbeingSlice.reducer;
